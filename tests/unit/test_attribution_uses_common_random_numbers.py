"""ACCEPTANCE TEST — ablation uses CRN

PHASE: 11
STATUS: not yet implemented. Skipped, NOT deleted: the acceptance criterion stays
visible in the repository from day one. Remove the skip and write the test as part
of Phase 11.
"""

import pytest

pytestmark = pytest.mark.skip(reason="PHASE 11 not yet implemented")


def test_attribution_uses_common_random_numbers():
    """ablation uses CRN"""
    raise NotImplementedError("PHASE 11")
